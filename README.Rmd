---
title: "README"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

# firstRepo

## Practice with Git

Check out this interactive tutorial and complete the first four levels. Once you have done that, recreate this document.

## R code

```{r simple figure, warning=FALSE}
library(tidyverse)
set.seed(273846)

dat <- data_frame(x = rnorm(100),
                  y = 2 + 3*x + rnorm(100))

ggplot(dat, aes(x, y)) +
    geom_point()

```

Here is another change.
